# Plan: Generate Seeded Ads JSON Dataset

## Goal
Generate 5 realistic classified marketplace ads in JSON format and write them to `src/app/seededAds.json`.

## Steps

1. **Write the JSON file** - Create `src/app/seededAds.json` with 5 ads following all rules:
   - Each ad has `main_image` + `slider_images` (3–5) with consistent naming prefixes
   - Realistic Nigerian titles, prices (₦), and descriptions
   - Nigerian seller names and 11-digit phone numbers (080/081/070/090)
   - Accurate category/subcategory pairs

2. **Verify homepage import** - Confirm `page.tsx` imports and uses the data correctly (already confirmed - line 12 imports `./seededAds.json`).

## File to modify
- `src/app/seededAds.json` — write the 5 ads
